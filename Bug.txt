*19/6/2026 — Session 65 tiếp 3 — Rollout dashboard-kit toàn analytics*
80. ✅ DONE: Áp SourceBadge (admin) vào header 15 trang analytics (Dashboard/BOD/all-time/channels/b2b/orders/staff/customers/vendors/products/fulfillment/3hk-usage/cs-troubleshoot/targets) + Dashboard thêm LogicNote MoM/dự phóng. Bỏ qua feedback (form) + sql (tool). tsc + build PASS.

*19/6/2026 — Session 65 tiếp 2 — Design system từ mockup B2C*
78. ✅ DONE: Tạo dashboard-kit.tsx (SourceBadge/LogicNote/DeltaPill/ValueStack/SnapshotMetric/Panel/DataReadiness) codify design language mockup — dùng chung toàn app.
79. ✅ DONE: Áp kit lên /analytics/b2c (flagship): Section thêm source badge + logic-note (công thức MTD/prorata/MoM, New vs Returning) + panel Data Readiness. tsc + build PASS.

*19/6/2026 — Session 65 tiếp — UX/UI cognitive load reduction (4 surface, mức an toàn)*
74. ✅ DONE: Sidebar — nhãn nhóm Analytics gray-300 (vô hình) → gray-400 10px; auto-mở nhóm Products khi đang ở trang SP (trước giấu mục active).
75. ✅ DONE: Chatbot — quick prompts gom 4 năng lực có nhãn (chunking); empty-state desc + placeholder phản ánh đủ năng lực (NCC catalog + doanh thu/đơn).
76. ✅ DONE: Analytics dashboard — KPI "Last period" số VND đầy đủ → rút gọn (glanceable), contrast tốt hơn, Việt hóa "Kỳ trước".
77. ✅ DONE: Admin — tab bar 3 cụm segmented thêm NHÃN cụm (Người dùng/Hệ thống/Công cụ) — trước không render dù có data.

*19/6/2026 — Session 65 — Đại tu chatbot: đọc đủ dữ liệu + tái cấu trúc agent*
69. ✅ FIXED: Chatbot "không biết NCC catalog" — searchNccWm match region text thô → 0 kết quả cho EU/Mỹ/Hàn/UAE, sót cho Thái. Thêm nccCountryScore() matcher (trực tiếp/khu vực/worldwide) → verify DB: 7 nước từ 0 → có; Thái 21→1737.
70. ✅ FIXED: Chatbot "không đọc doanh thu/đơn" — BI schema (agents.ts) sai/thiếu 7 bảng + tham chiếu bảng target_planning KHÔNG tồn tại. Viết lại schema chính xác 12 bảng gohub_dw. Test 3 query mẫu ra số thật.
71. ✅ FIXED: Lark bot KHÔNG xử lý bi-analyst (gọi Gemini thường, không có executeSQL) → BI không chạy trên Lark. Tách runBIAnalyst ra module chung (bi-analyst.ts), thêm nhánh BI vào lark/events.
72. ✅ FIXED: Overlap agent — ncc_catalog intent route về tu-van (tu-van ôm GoHub+NCC). Gộp NCC (browse+gap) về 1 chủ sở hữu gap-analysis ("NCC & Gap"); tu-van chỉ còn GoHub + 1 dòng trỏ NCC.
73. ✅ DONE: Nâng caps đọc dữ liệu vào context — searchSkus 15→40, searchSkusByGroupCode 20→50, NCC WM 20→40/50, KB 5→8.

*18/6/2026 — Session 64 tiếp 3 — Phân tích intel + SKU Destination UI + Partner Tiers UI*
66. ✅ DONE: Phân tích gohub-intel vs web. Gap settings: SKU Destination UI (#1 làm), Partner Tiers UI (#3 làm), Role data filters cho BI (chưa). Còn lại env-based/defer GA4. Report tabs khớp (B2C khác chủ đích).
67. ✅ DONE (#1): SKU Destination rule UI — POST /api/config/sku-destination-rule + SkuDestinationSection (prefix/offset/codeLength) trong Cài đặt.
68. ✅ DONE (#3): Partner Tiers UI dạng chip thay JSON textarea — thêm/xóa nhóm, chip partner có xóa, input thêm + datalist /api/channels. Giữ endpoint cũ.

*18/6/2026 — Session 64 tiếp — Pagination 20 + B2C font + B2B header + Gộp tab Phân quyền*
62. ✅ DONE: Phân trang 20 hàng/bảng danh sách. Component chung pager.tsx. Áp: products SKU, customers all-breakdown, staff leaderboard, cs-troubleshoot (perf + invalid). Đổi vendors 15→20, 3hk 50→20.
63. ✅ DONE: B2C font đậm dễ nhìn (giá trị bảng slate-800 semibold, KPI text-2xl, sub slate-500).
64. ✅ DONE: B2B header bố cục lại — icon chip Building2 + title + subtitle gọn, toolbar flex-wrap.
65. ✅ DONE: Gộp tab Danh sách + Phân quyền → "Người dùng & Phân quyền". Dropdown chọn account → Role + Phòng ban + box tích Analytics(16)/Tab PM(kb/skus/ncc) → nút Cập nhật. Ma trận global ở mục nâng cao. MỚI: per-user allowed_tabs (migration v17 ⚠️ CHẠY THỦ CÔNG) + sidebar override theo allowed_tabs.

*18/6/2026 — Session 64 — Port B2B y đúc + Cost + UserList ma trận + B2C redesign + Audit*
58. ✅ DONE: Audit merge gohub-intel → GoHub PM. 20/23 AVAILABLE_REPORTS đã port. Defer: Website Analytics (GA4), Schema Config. Dead code (đúng): OnDemand/Wholesale. B2C theo spec (chủ đích).
57. ✅ DONE: Port B2B tab Y ĐÚC (B2BPerformance.tsx) đầy đủ kể cả Cost + PDF. 5 KPI Actual+Projected, ComposedChart, Strategic tier expand, Other Tiers sort+expand, Export PDF, Manage Costs. API b2b/* rewrite trả GPM2/sub_channels/cost_breakdown. Channel Costs migrate Turso→Supabase (5 API mới + CostManagementModal). Deps jspdf+modern-screenshot.
56. ✅ DONE: UserList Analytics access — bỏ dropdown apply-ngay → ma trận ô CheckSquare/Square (giống tab Phân quyền), multi-select rồi bấm "Xác nhận cập nhật" mới lưu. Giữ allowed_analytics + endpoint.
55. ✅ DONE: B2C redesign theo format các tab khác — thêm 5 KPI cards + 2 chart (Area thị trường, Bar khách mới/quay lại), restyle bg-slate-50 card icon. Giữ API b2c/monthly + bảng rolling.

*18/6/2026 — Session 63 — tiếp 6 — UserList gán role+trang giống intel*
53. ✅ DONE: UserList (gán role + trang analytics per user) khó dùng → đổi card→BẢNG kiểu intel (User|Role|Phòng ban|Analytics|Thao tác) + search bar + đếm user. Analytics: dropdown checkbox cho staff/bod, badge Toàn quyền cho admin/manager. Giữ endpoint + logic allowed_analytics

*18/6/2026 — Session 63 — tiếp 5 — Format tab Phân quyền giống intel*
52. ✅ DONE: PermissionsTab + DeptTabMatrix đổi từ toggle pill → ma trận ô CheckSquare/Square style intel (header role viết hoa, icon box per feature). Giữ data model + endpoint /api/permissions + /api/admin/settings

*18/6/2026 — Session 63 — tiếp 4 — Redesign B2C theo spec*
51. ✅ DONE: Redesign trang B2C Apple-style 5 section theo gohub_b2c_dashboard_spec.md
    - API mới /api/analytics/b2c/monthly (rolling 6 tháng: VN/US/Total + New/Returning từ gohub_dw)
    - Section 1 (revenue rolling + MTD/MoM/prorata, tab Revenue/KPI) + Section 2 (customers) = data thật
    - Section 3-5 (CAC/Leads, GA4 charts, Budget/ROAS) = placeholder "chờ nguồn dữ liệu" (chưa có GA4/marketing)
    - Follow-up: sub-row Web/App/Sales cần mapping channel_name; KPI persist trong Settings chưa làm

*18/6/2026 — Session 63 — tiếp 3 — Port analytics khớp y đúc gohub-intel*
49. ✅ DONE: customers/staff/orders bị dịch VN + đổi style lúc port → port lại JSX nguyên văn từ intel (giữ data/API/tốc độ). motion.div→div (motion ko cài), API y hệt
50. ✅ DONE: bod "BOD Report"→"Board of Directors Report", b2b "B2B Performance"→"B2B Performance Summary"
    - Còn lại (dashboard/b2c/channels/fulfillment/products/all-time): đã trung thực sẵn

*18/6/2026 — Session 63 — tiếp 2 — Rename "Gohub Intel" + format tab admin*
47. ✅ DONE: Đổi tên web "GoHub PM"/"Gohub Telco" → "Gohub Intel" (layout title, sidebar brand, login). Subtitle → "Intelligence Hub"
48. ✅ DONE: Format tab admin — 8 tab phẳng → 3 cụm segmented (Người dùng/Hệ thống/Công cụ), header có icon chip + tên tab hiện tại, responsive icon-only mobile, mỗi tab vẫn 1-click

*18/6/2026 — Session 63 — tiếp — Nâng cấp chatbot (phân biệt nguồn + hỏi lại)*
43. ✅ FIXED: classifier thiếu intent bi_analytics/template_create → route sai. Bổ sung đủ 9 intent (thêm ncc_catalog, unclear)
44. ✅ FIXED: chatbot không phân biệt GoHub vs NCC catalog → thêm data_source (gohub_system/ncc_catalog/both) data-driven + header context tách bạch + directive [NGUỒN HỎI]
45. ✅ FIXED: chatbot chưa đọc được catalog NCC chi tiết → dataSource=ncc_catalog inject full WM ≤20 dòng + 3HK zones, mỗi dòng có trạng thái GoHub đã tạo/chưa tạo
46. ✅ ADDED: bước HỎI LẠI deterministic khi câu hỏi mơ hồ (thiếu nước/khu vực/mã) — web (badge Làm Rõ) + Lark (additive). Không gọi Gemini, hỏi đúng info còn thiếu

*18/6/2026 — Session 63 — QA/hardening analytics error messages*
41. ✅ FIXED: 13 trang analytics hiện lỗi thô (err.message/status code) → chuẩn hóa "Hiếu đang fix, vui lòng đợi" + console.error giữ debug. Dashboard bỏ dòng lộ "Kiểm tra Vercel env ANALYTICS_DB_*"
42. ✅ FIXED: orders/page.tsx + staff/page.tsx — silent failure: fetch lỗi không hiện gì (bảng rỗng). Thêm error state + catch + banner đỏ "Hiếu đang fix, vui lòng đợi"
    - Giữ nguyên có chủ đích: sql (admin SQL Explorer hiện lỗi SQL thật), cs-troubleshoot nút Sync/Migrate (feedback vận hành admin)

*18/6/2026 — Session 61 — Tester report (commit f5a2a4d)*
33. ✅ FIXED: Xóa folder analytics$dir/ rác (ký tự $ không hợp lệ, placeholder cũ)
34. ✅ FIXED: admin/page.tsx — roleBadgeClass thiếu bod (blue) và staff (teal) roles
35. N/A: All-Time date filter — hoạt động đúng với button "Áp dụng" (design intent)
36. ✅ FIXED: vendors/page.tsx — vendor dropdown không đóng khi click ngoài (thêm overlay)
37. ✅ FIXED: analytics/page.tsx + b2b/page.tsx — xóa local getDefaultDateRange/cn, import từ shared lib
38. ✅ FIXED: all-time/page.tsx — "Non-strategic" → "Non-Strategic" (casing nhất quán với codebase)
39. ✅ FIXED: migrate_turso_tickets.py — xóa hardcoded credentials, đọc từ env vars/.env.local

*16/6/2026 — Session 55 — Pending (template)*
31. ✅ FIXED (session 60 — commit f8661d1): Sửa quy trình tạo template — phân rõ thông tin nào tự fill, thông tin nào admin phải nhập
    - WM: dataPolicyCode chuyển sang Auto-fill, section labels rõ ràng hơn
    - 2 sections: "Bắt buộc nhập thủ công" + "Auto-fill từ SP đã chọn"

32. ✅ FIXED (session 60 — commit f8661d1): 3HK template có cả 2 loại SIM:
    - Checkbox "Top-up SIM" trong Step 2 3HK (default: unchecked)
    - Khi bật: mỗi combo generate 2 rows — eSIM (productType=C) + SIM (productType=E)
    - SIM row khác: typeOfSim="SIM", vendor_sku+"-SIM", productType="E"

*9/6/2026*
1. ✅ FIXED: về giá thì hãy sửa lại, là 1 cột là giá VND, 1 cột là giá USD nha
2. ✅ FIXED: nó vẫn hiện "Hệ thống đang xử lý, vui lòng thử lại sau." trên lark — add responseSent flag
3. ✅ FIXED: hiện tại nó đang giới hạn là 10 sản phẩm, hãy mở giới hạn là 50 sản phẩm

*9/6/2026 — APN Data Fix*
4. ✅ FIXED: SP Vendor APN missing — China, HK, Macao gói không có APN info
   - migration add_ncc_summary.sql ✓ chạy xong
   - import_worldmove_v2.py ✓ chạy xong
   - Web UI hiển thị APN summary (8 dòng header từ file)

*10/6/2026 — Session 21*
5. ✅ FIXED: Lark bot reply đúng rồi kèm thêm lỗi ⚠️ — xlsx .buffer.slice() throw sau khi card gửi xong. Fix: wrap xlsx try/catch, Buffer.from() thay .buffer.slice(), responseSent=true ngay sau send.
6. ✅ FIXED: Chatbot nói "không có nghe gọi" sai — call field không inject vào context, DATA_DICT thiếu call/hotspot, null≠"không hỗ trợ". Fix: thêm vào row SKU + DATA_DICT + DISPLAY_RULES.
7. ✅ FIXED: Lark bot mất ngữ cảnh bảng — lưu "[bảng đã gửi kèm]" thay full response. Fix: lưu full Gemini response vào lark_chat_history.
8. ✅ FIXED: Lark "First content should be with role user, got model" — HISTORY_LIMIT cắt sai. Fix: trim leading assistant messages + ensure alternating.
9. ✅ FIXED: searchSkus bỏ sót gói unlimited (is_unlimited flag không đồng bộ data_amount=9999) + eSIM filter không pass. Fix: || data_amount>=9999, pass sim_type.

*10/6/2026 — Session 22 (Lark group + thread)*
10. ✅ FIXED: Lark bot không trả lời group mới khi add nhiều cùng lúc — im.chat.member.bot.added_v1 bị ignore. Fix: handle bot_added event → gửi welcome message.
11. ✅ FIXED: Lark group @mention name mismatch — exact match fails. Fix: toLowerCase + trim + includes fallback.
12. ✅ FIXED: Lark group @mention không hoạt động — msgType="post" (rich text) bị skip, chỉ xử lý "text". Fix: parse post content format (tag="at"/"text" nested JSON).
13. ✅ FIXED: Lark bot không reply trong thread — root_id != message_id nhưng vẫn phải @mention. Fix: isInThread → skip @mention check, reply tất cả message trong thread.
14. ✅ ADDED: Tỷ giá T06/2026 đầy đủ 11 currencies (SQL migration + admin UI labels).

*10/6/2026 — Session 24*
15. ✅ FIXED: Lark bot không reply khi @mention trên thread (mention tại root/top của thread).
    Root cause chuỗi:
    a. threadFallback chỉ chạy khi isInThread=true → commit 78b20eb (rootMsgFallback)
    b. User chỉ gõ "@Bot" không kèm text → userText="" → skip — commit b6c5260
    c. [ROOT CAUSE THẬT] Content format thread: {"title":"","content":[[...]]} không có wrapper "zh_cn"
       → Object.values()[0] lấy title="" thay vì content array → postMentions=0, userText="" → skip
       → Fix: body = rawContent.zh_cn ?? rawContent.en_us ?? rawContent — commit e23da2e
    Lesson: 2 format post Lark khác nhau — group cũ dùng zh_cn wrapper, thread dùng root-level content
16. ✅ DONE: Business rules inject vào chatbot (tu-van + gap-analysis) — commit e41b750
    - Combo chuẩn 42 combo/country, vendor priority (WM/3HK/KDDI), thuật ngữ “cần request vendor”

*13/6/2026 — Pending (session tiếp theo)*

*14/6/2026 — Session 51 (bugs mới)*
26. ✅ FIXED: Phân quyền phòng ban: sidebar fetch dept từ /api/user/me (DB trực tiếp), không dùng JWT → refresh trang là cập nhật ngay
27. ✅ FIXED: SP Hệ Thống Tab Products rỗng — xóa data_plan_type khỏi SELECT_COLS (cột đã DROP từ session 35)
28. ✅ FIXED: SP Hệ Thống — ItemsTable: thêm Chi tiết button (19 fields modal), bỏ bớt cột ít dùng, truncate item_name_vn >40 chars + SKU note >60 chars
29. ✅ FIXED: Tab Thông tin — thêm sub-tab "Category" (ref_categories: mã, tên EN/VN, ISO code, loại đơn/đa)
30. ✅ FIXED: Chatbot mã nhóm nước AP2 (commit 2fb23d7)
    - Root cause: router split "cho tôi biết AP2" → "CHO" match regex trước "AP2" → groupCode="CHO"
    - Fix: bỏ qua word thuần chữ thường /^[a-z]+$/ khi detect groupCode (router.ts)

*14/6/2026 — Session 50*
21. ✅ FIXED: Tab Khuyến Mãi — admin có cột SKU count; view bỏ cột Perks VN; date trống → "-"
22. ✅ FIXED: Wiki admin — click dept badge trong list view để đổi phòng ban inline
23. ✅ FIXED: RBAC phòng ban — migration v11 (cần chạy), sidebar lọc tabs theo dept, admin gán dept per user
24. ✅ FIXED: Admin Phân Quyền — matrix phòng ban × tab (KB/SKUs/NCC) trong PermissionsTab
25. ✅ FIXED: Export XLSX — SP Hệ Thống (Products/SKUs/Items tabs) + SP Vendor WM
17. ✅ FIXED: UX confirm delete — fade-in backdrop + scale-in modal (CSS keyframes), loading spinner + “Đang xóa...” trên nút confirm, disable cả 2 nút khi đang xóa. Áp dụng cho KB docs, Wiki pages, Admin users.
18. ✅ FIXED: Wiki ẩn/hiện — cột is_hidden trong DB (migration v9_wiki_hidden.sql, cần chạy thủ công trong Supabase), API filter cho non-admin, admin có nút EyeOff/Eye per row + badge “Ẩn” trên title.
19. ✅ FIXED: Wiki inline search — nhấn Enter trong search bar của WikiTab → gọi /api/kb/search lọc source=wiki, hiện “Kết quả ngữ nghĩa” bên dưới list với % khớp.
20. ✅ FIXED: Re-embed 13 wiki pages — OK=13, FAIL=0. Chatbot giai-dap giờ search được wiki.

*18/6/2026 — Session 62 (2026-06-18)*
40. ✅ FIXED: sidebar.tsx — đổi tất cả text tiếng Việt sang tiếng Anh: "Quản Lý"→"Management", "Sản Phẩm"→"Products", "Báo Cáo & BI"→"Reports & BI", "Mở rộng/Thu gọn sidebar"→"Expand/Collapse sidebar", "Đăng xuất"→"Sign Out" (commit fcef01c)
